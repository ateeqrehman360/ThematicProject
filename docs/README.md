First make sure to download those tools or files before you run anything in the backend:
- Node.js (LTS) last version
- Docker Desktop

Install inside the Command Prompt (CMD):
npm install

And the command to run the supabase is:
- npm run supabase:start
to check if is run properly 
- npm run supabase:status